[defaults]
# Search for roles in both the cache (galaxy-installed) and local project roles
roles_path = /cache/ansible-roles:./roles

# Performance optimizations (also set in CI env vars but explicit here for local runs)
host_key_checking = False
pipelining = True
