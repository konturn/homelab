# Prometheus Workspace - Git Ignore
# Order matters: gitignore uses last-match-wins

# ── Ignore everything by default ──
*

# ── Allow descending into directories ──
!*/

# ── Track markdown files everywhere ──
!*.md

# ── Track specific config/state JSON ──
!config/cron-jobs.json
!memory/*.json
!life/**/items.json

# ── Track job hunting data ──
!skills/job-hunting/hardened.json
!skills/job-hunting/references/*.json
!skills/job-hunting/references/**/*.json

# ── Track scripts in skills and tools ──
!skills/**/scripts/*.sh
!skills/**/scripts/*.py
!skills/**/scripts/*.js
!skills/**/lib.sh
!tools/*.sh
!tools/*.md

# ── Track specific skill configs ──
!skills/face-recognition/models/**
!skills/face-recognition/package.json
!skills/moltbook/package.json
!skills/imap-email/package.json
!skills/imap-email/.clawhub/origin.json

# ── Track other essentials ──
!.gitignore
!avatars/*

# ── Hard excludes (override everything above) ──
node_modules/
homelab/
homelab-*/
images-repo/
tmp/
canvas/
.keys/
*.log
*.jsonl

# ── Exclude credentials from git ──
memory/moltbook-credentials.json
