stages:
  - test
build:
  stage: test
  script:
    - apt-get update
    - apt-get -y install python3 python3-nacl python3-pip libffi-dev
    - apt install -y dnsutils
    - dig router.lab.nkontur.com
    - docker-compose --file docker/docker-compose.yml --project-directory docker up -d
